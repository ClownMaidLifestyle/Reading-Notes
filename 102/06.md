## javascript

Console - In-browser 
- write Js directly into it

#### Variables

**let**
i.e ``let Bungus = "bingus"``
use instead of var
variable but, better than var
doesn't need a value to be declared

**const**

constant
i.e ``const needs = "tea"``


**var**
ie. ``var Dinosaur = "Oswald"``  
Var is bad and icky, not best practise

***

#### Cool Commands

Console.log - check what a variable is
alert("") - Sends a pop-up with a message
console.assert((x == 1), {value: x, errormessage: "hey this stuff is messed up to the max"}) - query a variable and return a message if returns FALSE
console.clear - clears the console (duh)
**`//` for comments**

#### Toggle device viewport
An option in the console to view the site on different devices
